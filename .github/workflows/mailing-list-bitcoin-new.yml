name: Bitcoin-dev Mailing list
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      migration_mode:
        description: 'Migration mode'
        required: true
        default: 'test'
        type: choice
        options:
        - test
        - year
        - full
        - dry_run
      migration_year:
        description: 'Year to process (for year mode)'
        required: false
        default: '2025'
      dry_run:
        description: 'Dry run mode (preview only)'
        required: false
        default: false
        type: boolean
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Fetch data
        run: |
          python mailing-list/main.py
        env:
            ES_ENGINE: ${{ secrets.ES_ENGINE }}
            ES_URL: ${{ secrets.ES_URL }}
            ES_TOKEN: ${{ secrets.ES_TOKEN }}
            CLOUD_ID: ${{ secrets.CLOUD_ID }}
            USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
            USERNAME: ${{ secrets.USERNAME }}
            INDEX: ${{ secrets.INDEX }}
            DATA_DIR: /tmp/data
            MIGRATION_MODE: ${{ github.event.inputs.migration_mode || 'test' }}
            MIGRATION_YEAR: ${{ github.event.inputs.migration_year || '2025' }}
            DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
